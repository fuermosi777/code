/**
 * For many points (object of {x: 1, y: 2}), find all the points which fall in r radius of a given point.
 */

/**
 * @param  {object[]} points All points
 * @param  {object} p The point
 * @param  {number} r Radius
 * @return {object[]}
 */
function find(points, p, r) {
  // iterate all points...
}

/**
 * Follow up: how to increase the performance?
 *
 * Divide the map into small pieces.
 */